# HQT C++ Core Engine

# Commented out until actual source files exist
# add_library(hqt_core
#     src/core/event_loop.cpp
#     src/core/engine.cpp
#     # ... more sources as they're created
# )

# target_include_directories(hqt_core
#     PUBLIC
#         $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
#         $<INSTALL_INTERFACE:include>
# )

# target_link_libraries(hqt_core
#     PUBLIC
#         spdlog::spdlog
#         cppzmq
#         hdf5::hdf5
#         tomlplusplus::tomlplusplus
# )

message(STATUS "C++ core library will be built here once source files are added")
